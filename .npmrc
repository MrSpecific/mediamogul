# @wlcr/base-ic is published to the public npm registry. This overrides any
# global ~/.npmrc that routes the @wlcr scope to GitHub Packages, so installs
# work for everyone on the team without a GitHub Packages token.
@wlcr:registry=https://registry.npmjs.org
